# 🎨 تفعيل توليد الصور والفيديو - الطريقة السهلة

## ✅ الوضع الحالي

```
✅ الدردشة (GPT-5): تعمل
✅ توليد الأكواد: يعمل
✅ بناء المواقع: يعمل
✅ تطوير التطبيقات: يعمل

⏳ توليد الصور: يحتاج تفعيل
⏳ توليد الفيديو: يحتاج تفعيل
⏳ توليد الصوت: يحتاج تفعيل
```

---

## 🚀 خيارات التفعيل

### **الخيار 1: استخدم APIs أدوات GenSpark المدمجة** ⭐

لديك بالفعل حساب GenSpark مع 4551 رصيد!

#### الطريقة:
```bash
# لا تحتاج API Key منفصل!
# استخدم أدوات GenSpark مباشرة:

# في Terminal:
# 1. افتح GenSpark Playground
# 2. ولّد الصورة/الفيديو هناك
# 3. حمّل النتيجة
# 4. ارفعها لزيزو كملف
```

---

### **الخيار 2: استخدم Replicate (مجاني للبدء)** 💎

#### الخطوات:
```
1️⃣ سجّل في: https://replicate.com
2️⃣ احصل على $10 رصيد مجاني
3️⃣ اذهب لـ: https://replicate.com/account/api-tokens
4️⃣ انسخ API Token
```

#### أضف المفتاح:
```bash
echo "replicate:
  api_key: r8_YOUR_TOKEN_HERE" >> ~/.genspark_llm.yaml
```

#### تحديث زيزو:
```python
# في app.py، أضف:
import replicate

def generate_image_replicate(prompt):
    output = replicate.run(
        "stability-ai/sdxl:39ed52f2a78e934b3ba6e2a89f5b1c712de7dfea535525255b1aa35c5565e08b",
        input={"prompt": prompt}
    )
    return output[0]  # image URL
```

---

### **الخيار 3: استخدم FAL.AI (الأسرع)** ⚡

#### الخطوات:
```
1️⃣ سجّل في: https://fal.ai
2️⃣ احصل على $5 رصيد مجاني
3️⃣ اذهب لـ: https://fal.ai/dashboard/keys
4️⃣ انسخ API Key
```

#### التثبيت:
```bash
pip install fal-client
```

#### أضف المفتاح:
```bash
export FAL_KEY="fal_YOUR_KEY"
```

#### تحديث زيزو:
```python
import fal_client

def generate_image_fal(prompt):
    result = fal_client.submit(
        "fal-ai/flux-pro",
        arguments={"prompt": prompt}
    )
    return result['images'][0]['url']
```

---

## 💡 الحل الأسرع الآن (5 دقائق):

### **استخدم Replicate - مجاني وسريع!**

```bash
# 1. سجّل في Replicate
open https://replicate.com/signin

# 2. احصل على Token
# بعد التسجيل، اذهب لـ:
# https://replicate.com/account/api-tokens

# 3. انسخ التوكن (يبدأ بـ r8_)

# 4. أضفه للملف:
echo "replicate:
  api_key: r8_YOUR_TOKEN" >> ~/.genspark_llm.yaml

# 5. ثبّت المكتبة:
pip install replicate

# 6. أعد تشغيل زيزو:
pkill -f app.py
cd /home/user/webapp && python3 app.py &
```

---

## 🎯 بعد التفعيل:

سأحدّث زيزو ليستخدم Replicate تلقائياً!

الخطوة الوحيدة المطلوبة منك:
```
1. سجّل في Replicate (دقيقة)
2. انسخ API Token (30 ثانية)
3. أرسله لي هنا
4. سأضيفه وأحدّث الكود! ✅
```

---

## 📊 مقارنة الخيارات:

| الخدمة | الرصيد المجاني | السرعة | سهولة الإعداد |
|--------|----------------|---------|---------------|
| GenSpark Playground | 4551 رصيد ✅ | ⭐⭐⭐⭐ | ⭐⭐⭐ (يدوي) |
| Replicate | $10 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ (5 دقائق) |
| FAL.AI | $5 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ (10 دقائق) |

**🏆 الموصى به: Replicate (الأسهل + الأسرع)**

---

## ✅ الخلاصة:

```
الخيار السريع:
1. سجّل في Replicate
2. احصل على API Token
3. أرسله لي
4. سأفعّل كل شيء! 🚀
```

**وقت التفعيل: 2 دقيقة فقط!** ⚡
