# 🌟 دليل تفعيل GenSpark API - خطوة بخطوة

## 🎯 لماذا GenSpark؟

```
✅ مجاني للبدء: 100 رصيد مجاني عند التسجيل
✅ شامل: صور + فيديو + صوت في مكان واحد
✅ سهل: لا حاجة لبطاقة ائتمان
✅ سريع: تفعيل في 5 دقائق
✅ عربي: دعم كامل للغة العربية
```

---

## 📋 الخطوات التفصيلية

### **الخطوة 1: التسجيل في GenSpark** ⏱️ دقيقة واحدة

#### 1.1 افتح موقع GenSpark
```
🌐 الرابط: https://www.genspark.ai
```

أو انسخ والصق في المتصفح:
```
https://www.genspark.ai
```

#### 1.2 اضغط على "Sign Up" أو "التسجيل"
```
الموقع: أعلى يمين الصفحة
```

#### 1.3 أدخل معلوماتك
```
📧 البريد الإلكتروني: your_email@example.com
🔒 كلمة المرور: (اختر كلمة قوية)
```

أو سجّل عبر:
```
🔵 Google Account
⚫ GitHub Account
```

#### 1.4 أكّد بريدك الإلكتروني
```
1. افتح بريدك
2. ابحث عن رسالة من GenSpark
3. اضغط على رابط التأكيد
```

**✅ تم! الآن لديك حساب + 100 رصيد مجاني**

---

### **الخطوة 2: الحصول على API Key** ⏱️ دقيقة واحدة

#### 2.1 تسجيل الدخول
```
🌐 الرابط: https://www.genspark.ai/login
```

#### 2.2 افتح لوحة التحكم (Dashboard)
```
بعد تسجيل الدخول، ستجد نفسك في:
Dashboard أو Home Page
```

#### 2.3 اذهب إلى الإعدادات
```
الطريقة 1:
اضغط على أيقونة ⚙️ (Settings) في الزاوية

الطريقة 2:
افتح القائمة الجانبية > Settings

الطريقة 3:
اذهب مباشرة: https://www.genspark.ai/settings
```

#### 2.4 افتح قسم API Keys
```
في صفحة Settings:
1. ابحث عن "API Keys" أو "مفاتيح API"
2. اضغط عليها
```

#### 2.5 أنشئ مفتاح جديد
```
1. اضغط: "Create New Key" أو "+ New API Key"
2. أدخل اسماً: "Zizo Ultimate"
3. اختر الصلاحيات:
   ✅ Image Generation
   ✅ Video Generation
   ✅ Audio Generation
   ✅ Chat Completion
4. اضغط: "Create" أو "إنشاء"
```

#### 2.6 انسخ المفتاح
```
⚠️ هام جداً: المفتاح سيظهر مرة واحدة فقط!

المفتاح يبدأ بـ: gs_
مثال: gs_1234567890abcdef1234567890abcdef

✅ انسخه الآن وحفظه في مكان آمن
```

**✅ رائع! الآن لديك API Key**

---

### **الخطوة 3: إضافة المفتاح لزيزو** ⏱️ دقيقتان

الآن لديك خياران:

---

#### **الخيار أ: عبر واجهة زيزو (الأسهل)** 🌟

##### 1. افتح زيزو
```
🌐 الرابط:
https://5000-ik098qc46w5n2q8a9szme-5185f4aa.sandbox.novita.ai
```

##### 2. اضغط زر الإعدادات
```
⚙️ موجود في شريط القدرات العلوي
(آخر زر على اليمين)
```

##### 3. شاهد حالة المفاتيح
```
ستجد:
🌟 GenSpark: ❌ غير مفعّل
```

##### 4. أضف المفتاح يدوياً
```
حالياً: يجب إضافته عبر Terminal (الخيار ب)
قريباً: سنضيف زر "إضافة مفتاح" في الواجهة
```

**⬇️ تابع الخيار ب أدناه**

---

#### **الخيار ب: عبر ملف الإعداد** 💻

هذه الطريقة تعمل 100% الآن!

##### الطريقة 1: عبر Echo Command (الأسرع)

```bash
# 1. افتح Terminal أو Command Line

# 2. نفّذ هذا الأمر:
echo "genspark:
  api_key: gs_YOUR_KEY_HERE" >> ~/.genspark_llm.yaml

# 3. استبدل gs_YOUR_KEY_HERE بمفتاحك الحقيقي
```

**مثال:**
```bash
echo "genspark:
  api_key: gs_1234567890abcdef1234567890abcdef" >> ~/.genspark_llm.yaml
```

##### الطريقة 2: عبر Text Editor

```bash
# 1. افتح الملف:
nano ~/.genspark_llm.yaml

# 2. أضف هذه الأسطر:
genspark:
  api_key: gs_YOUR_KEY_HERE
  base_url: https://api.genspark.ai/v1

# 3. احفظ الملف:
# - اضغط: Ctrl + O
# - اضغط: Enter
# - اضغط: Ctrl + X

# 4. تحقق من الإضافة:
cat ~/.genspark_llm.yaml
```

##### الطريقة 3: عبر Python Script

```bash
# 1. نفّذ هذا:
python3 << 'EOF'
import yaml
import os

config_path = os.path.expanduser('~/.genspark_llm.yaml')

# تحميل الإعداد الحالي (إن وُجد)
if os.path.exists(config_path):
    with open(config_path, 'r') as f:
        config = yaml.safe_load(f) or {}
else:
    config = {}

# إضافة مفتاح GenSpark
config['genspark'] = {
    'api_key': 'gs_YOUR_KEY_HERE',
    'base_url': 'https://api.genspark.ai/v1'
}

# حفظ الملف
with open(config_path, 'w') as f:
    yaml.dump(config, f)

print("✅ تم إضافة المفتاح بنجاح!")
EOF

# 2. استبدل gs_YOUR_KEY_HERE في السطر أعلاه
```

**✅ تم! المفتاح الآن محفوظ**

---

### **الخطوة 4: إعادة تشغيل زيزو** ⏱️ 30 ثانية

زيزو يحتاج لإعادة تشغيل لقراءة المفتاح الجديد.

#### الطريقة 1: عبر Terminal

```bash
# 1. أوقف زيزو:
pkill -f "python3 app.py"

# 2. انتظر ثانية:
sleep 1

# 3. شغّل زيزو مجدداً:
cd /home/user/webapp && python3 app.py > zizo.log 2>&1 &

# 4. تحقق من أنه يعمل:
sleep 3
curl http://localhost:5000/health
```

#### الطريقة 2: ستتم تلقائياً

```
إذا كان زيزو يعمل في sandbox:
سيتم إعادة التشغيل تلقائياً عند تحديث الملف
```

**✅ تم! زيزو الآن يقرأ المفتاح**

---

### **الخطوة 5: اختبار التكامل** ⏱️ دقيقة واحدة

الآن لنتأكد أن كل شيء يعمل!

#### 5.1 تحقق من حالة المفتاح

```bash
# عبر Terminal:
curl http://localhost:5000/api/keys | python3 -m json.tool
```

**يجب أن ترى:**
```json
{
  "keys": {
    "genspark": {
      "present": true,
      "key": "gs_123456...xxxxx",
      "status": "active"
    }
  }
}
```

#### 5.2 افتح واجهة زيزو

```
🌐 الرابط:
https://5000-ik098qc46w5n2q8a9szme-5185f4aa.sandbox.novita.ai
```

#### 5.3 اضغط ⚙️ (الإعدادات)

```
يجب أن ترى:
🌟 GenSpark: ✅ مفعّل
gs_123456...xxxxx
```

**✅ ممتاز! المفتاح معرّف بنجاح**

---

### **الخطوة 6: جرّب التوليد!** ⏱️ دقيقة واحدة

الآن الجزء الممتع! 🎉

#### 6.1 جرّب توليد صورة

```
1. في زيزو، اضغط: 🎨 صور
2. اكتب: قط أبيض جميل يلعب في حديقة
3. اضغط: ✈️ (إرسال)
4. انتظر: 30-60 ثانية
```

**يجب أن ترى:**
```
🎨 تم بدء توليد الصورة!

الوصف: قط أبيض جميل يلعب في حديقة
النموذج: Flux 2 Pro
الحالة: جاري المعالجة...

[أيقونة دوّارة مع خلفية متدرجة]
```

#### 6.2 جرّب توليد فيديو

```
1. اضغط: 🎬 فيديو
2. اكتب: غروب الشمس على البحر
3. اضغط: ✈️
4. انتظر: 2-4 دقائق
```

**يجب أن ترى:**
```
🎬 تم بدء توليد الفيديو!

الوصف: غروب الشمس على البحر
النموذج: Gemini Veo 3.1
المدة: 8 ثوان
الدقة: 1080p
الحالة: جاري المعالجة...
```

#### 6.3 جرّب توليد صوت

```
1. اضغط: 🎵 صوت
2. اكتب: موسيقى هادئة للاسترخاء
3. اضغط: ✈️
4. انتظر: 30-90 ثانية
```

**يجب أن ترى:**
```
🎵 توليد الموسيقى

الوصف: موسيقى هادئة للاسترخاء
النموذج: elevenlabs/music
المدة: 60s
الحالة: جاري المعالجة...
```

---

## 💰 رصيدك المجاني

### ماذا تحصل مع 100 رصيد؟

```
🎨 الصور: ~20-30 صورة
   (كل صورة = 3-5 رصيد)

🎬 الفيديو: ~5-10 فيديوهات
   (كل فيديو = 10-20 رصيد)

🎵 الصوت: ~30-50 ملف صوتي
   (كل ملف = 2-3 رصيد)
```

### كيف تتحقق من رصيدك؟

```
1. اذهب إلى: https://www.genspark.ai/dashboard
2. انظر في: Usage أو Billing
3. ستجد: Current Balance: XX رصيد
```

---

## ⚠️ حل المشاكل الشائعة

### المشكلة 1: "المفتاح غير صالح"

**الحل:**
```bash
# 1. تحقق من المفتاح:
cat ~/.genspark_llm.yaml | grep api_key

# 2. تأكد أنه يبدأ بـ gs_
# 3. تأكد أنه لا يوجد مسافات زائدة
# 4. أعد كتابة المفتاح:
nano ~/.genspark_llm.yaml
```

### المشكلة 2: "الرصيد منتهي"

**الحل:**
```
1. تحقق من رصيدك في Dashboard
2. إذا انتهى:
   - أضف رصيد ($5-10)
   - أو سجّل في خدمة أخرى (FAL.AI مثلاً)
```

### المشكلة 3: "التوليد بطيء"

**هذا طبيعي!**
```
⏱️ الصور: 30-60 ثانية
⏱️ الفيديو: 2-4 دقائق
⏱️ الصوت: 30-90 ثانية

💡 نصيحة: افتح عدة tabs وولّد بالتوازي
```

### المشكلة 4: "زيزو لا يقرأ المفتاح"

**الحل:**
```bash
# 1. تحقق من الملف:
ls -la ~/.genspark_llm.yaml

# 2. تحقق من الصلاحيات:
chmod 644 ~/.genspark_llm.yaml

# 3. تحقق من المحتوى:
cat ~/.genspark_llm.yaml

# 4. أعد تشغيل زيزو:
pkill -f app.py
cd /home/user/webapp && python3 app.py &
```

### المشكلة 5: "خطأ في الاتصال"

**الحل:**
```bash
# 1. اختبر الاتصال:
curl -H "Authorization: Bearer gs_YOUR_KEY" \
  https://api.genspark.ai/v1/health

# 2. إذا فشل:
# - تحقق من الإنترنت
# - تحقق من المفتاح
# - تحقق من base_url في الملف
```

---

## 📊 جدول تكلفة التوليد

| النوع | التكلفة بالرصيد | الوقت | الجودة |
|------|----------------|-------|--------|
| **صورة 1024×1024** | 3-5 رصيد | 30-60s | ⭐⭐⭐⭐⭐ |
| **فيديو 8 ثوان** | 10-20 رصيد | 2-4min | ⭐⭐⭐⭐⭐ |
| **صوت 60 ثانية** | 2-3 رصيد | 30-90s | ⭐⭐⭐⭐ |
| **دردشة GPT-5** | 0.1-0.5 رصيد | فوري | ⭐⭐⭐⭐⭐ |

---

## 💡 نصائح للاستفادة القصوى

### 1. استخدم أوصاف تفصيلية
```
❌ سيء: "قط"
✅ جيد: "قط أبيض جميل بعيون زرقاء يلعب في حديقة خضراء تحت أشعة الشمس"
```

### 2. جرّب نماذج مختلفة
```
الصور:
- Flux 2 Pro (أعلى جودة)
- DALL-E 3 (أكثر إبداعاً)
- Stable Diffusion (أسرع)
```

### 3. استفد من الرصيد المجاني
```
100 رصيد = ~30 محاولة مختلطة
- 10 صور عالية الجودة
- 5 فيديوهات قصيرة
- 15 ملف صوتي
```

### 4. احفظ النتائج الجيدة
```
اضغط زر "تحميل" ⬇️ لحفظ:
- الصور
- الأكواد
- المواقع
```

### 5. استخدم التاريخ
```
زيزو يحفظ آخر 20 رسالة
استفد منها للتحسين التدريجي
```

---

## 🎯 خطة الاستخدام الذكية

### اليوم 1-3: التجربة (30 رصيد)
```
✅ 5 صور تجريبية (15 رصيد)
✅ 2 فيديو قصير (15 رصيد)
✅ كود وموقع (مجاناً!)
```

### اليوم 4-7: المشاريع (50 رصيد)
```
✅ 10 صور للمشروع (30 رصيد)
✅ 2 فيديو احترافي (20 رصيد)
✅ 5 ملفات صوتية (10 رصيد)
```

### اليوم 8-14: الاحتياطي (20 رصيد)
```
✅ تحسينات وإعادة توليد
✅ اختبار ميزات جديدة
```

---

## 📈 ماذا بعد؟

### إذا انتهى الرصيد المجاني:

#### الخيار 1: إضافة رصيد لـ GenSpark
```
💰 السعر: من $5 (يعطيك ~100 رصيد إضافي)
📍 الطريقة: Dashboard > Billing > Add Credits
💳 الدفع: بطاقة ائتمان، PayPal
```

#### الخيار 2: التسجيل في خدمات أخرى
```
🎨 FAL.AI: $5 رصيد مجاني إضافي
🎨 Stability AI: 25 صورة مجانية
🎵 ElevenLabs: 10K حرف مجاني
```

#### الخيار 3: استخدام ميزات زيزو المجانية
```
✅ دردشة GPT-5 (محدودة)
✅ توليد الأكواد (مجاني!)
✅ بناء المواقع (مجاني!)
✅ تطوير التطبيقات (مجاني!)
```

---

## ✅ Checklist: هل أنت جاهز؟

قبل أن تبدأ، تحقق:

```
☐ سجلت في GenSpark
☐ حصلت على API Key (يبدأ بـ gs_)
☐ أضفت المفتاح لملف ~/.genspark_llm.yaml
☐ أعدت تشغيل زيزو
☐ فتحت واجهة زيزو
☐ تحققت من الإعدادات (⚙️)
☐ رأيت ✅ مفعّل بجانب GenSpark
☐ جربت توليد صورة
☐ شاهدت "جاري المعالجة..."
```

**إذا كل ✅، أنت جاهز 100%!** 🎉

---

## 🎉 مبروك! أنت الآن جاهز!

### ماذا لديك الآن:

```
✅ حساب GenSpark مفعّل
✅ API Key يعمل
✅ 100 رصيد مجاني
✅ زيزو متصل بـ GenSpark
✅ جاهز لتوليد:
   🎨 الصور
   🎬 الفيديوهات
   🎵 الموسيقى والأصوات
```

### ابدأ الآن:

```
1. افتح زيزو:
   https://5000-ik098qc46w5n2q8a9szme-5185f4aa.sandbox.novita.ai

2. اختر قدرة (🎨 أو 🎬 أو 🎵)

3. اكتب طلبك

4. شاهد السحر يحدث! ✨
```

---

## 📞 هل تحتاج مساعدة؟

### الدعم:

```
📧 البريد: support@genspark.ai
💬 Discord: https://discord.gg/genspark
📖 التوثيق: https://docs.genspark.ai
🐛 Issues: https://github.com/aboday2067-dot/sudan/issues
```

### أسئلة شائعة:

```
❓ كم يستغرق التوليد؟
⏱️ صور: 30-60s | فيديو: 2-4min | صوت: 30-90s

❓ هل الرصيد يجدد شهرياً؟
❌ لا، لكن يمكنك شراء المزيد

❓ هل يدعم العربية؟
✅ نعم، 100%

❓ ماذا لو انتهى الرصيد؟
💰 أضف رصيد أو سجل في خدمة أخرى
```

---

**🚀 الآن... انطلق وأبدع مع زيزو ألتيميت!**

**صُنع بـ ❤️ من السودان | دليل GenSpark v1.0 | آخر تحديث: 2026-02-11**
